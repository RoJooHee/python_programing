class 부모클래스이름(object):
    def __init__(self, aaa, wallet):
        self.aaa=aaa
        self.__wallet=wallet

    def pat(self, amount):
        self.__wallet=wallet #apthemfhaks wjqrmsrksmd.
    def ~ (self, 매개변수들):
        return

stu = 클래스이름(name, id, password, profile) #객체는 stu
student_list.append(stu)

class 자식클래스이름(부모클래스이름):
    def__init__(self, aaa, wallet, 추가하고싶은거):
        super().__init__(nam)
for i in range(len(student_list)): #for문 돌면서 id,password가 맞는 객체 찾기
    login_yn = student_list[i].함수이름(매개변수들)

f=open("~.txt", "r")
contents=f.read()
f.close()

with open("~.txt", "r") as f
    contents=f.read()

with open("~.csv") as c:
    while1:
